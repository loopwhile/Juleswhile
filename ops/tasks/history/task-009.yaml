tasks:
  - kind: task
    id: TASK-009
    title: Live Jules Session production pilot
    role: verifier
    type: verification
    status: COMPLETED
    priority: P0
    enabled: false
    objective: Run one live production pilot through the canonical Jules Dispatcher after TASK-008 hardening is completed in production, creating exactly one real Jules Session and verifying the PR, CI, Custom Auto Merge, Netlify Production, and completed lifecycle path.
    depends_on:
      - TASK-008
    inputs:
      - AGENTS.md
      - PROJECT_GOAL.md
      - operator-guide.md
      - GitHub Issue
      - docs/07_operations/operational-hardening.md
      - .github/workflows/02-dispatch-jules.yml
      - ops/tasks/task-index.yaml
      - ops/state/project-state.json
    outputs:
      - docs/07_operations/live-jules-pilot-2026-06-29.md
    acceptance_criteria:
      - Exactly one real Jules Session is created through the canonical Dispatcher.
      - A quota reservation marker exists before Session creation.
      - A dispatch intent marker exists before Session creation.
      - The quota reservation is committed with the Session ID.
      - Exactly one canonical Session marker exists.
      - Duplicate Session count is zero.
      - Jules changes only docs/07_operations/live-jules-pilot-2026-06-29.md.
      - PR marker and TASK Issue reference are correct.
      - CI succeeds.
      - Custom Auto Merge succeeds after owner approval policy conditions are met.
      - Netlify Production succeeds.
      - Issue transitions to state:completed with closed reason completed.
      - Projection returns active Session 0.
      - No unresolved dispatch outcome remains.
    allowed_paths:
      - docs/07_operations/live-jules-pilot-2026-06-29.md
    forbidden_paths:
      - .github/**
      - ops/**
      - package.json
      - package-lock.json
      - netlify.toml
      - src/**
      - content/**
      - public/**
      - PROJECT_GOAL.md
      - AGENTS.md
    forbidden_actions:
      - Read or print Secret values.
      - Modify workflows.
      - Create another TASK.
      - Create another Jules Session.
      - Push directly to main.
      - Change dependencies.
      - Change Netlify settings.
      - Change authentication, payment, or credential policy.
    validation_commands:
      - npm run validate:schemas
      - npm run validate:task-graph
      - npm run validate:supply-chain
      - npm run lint
      - npm test
      - npm run build
      - npm run ci
    risk_level: low
    approval_policy: human
    parallelizable: false
    resource_locks:
      - live-jules-pilot
      - jules-dispatch
      - runtime-state
    conflicts_with: []
    retry_policy:
      max_corrections: 2
      timeout_minutes: 90
    stitch:
      allowed: false
      required: false
      expected_outputs: []
    metadata:
      goal_issue_number: 1
      issue_number: 26
      created_at: 2026-06-29T10:50:01Z
      updated_at: 2026-06-29T12:02:01Z
      created_by: user
      tags:
        - live-pilot
        - jules
        - production
        - verification
